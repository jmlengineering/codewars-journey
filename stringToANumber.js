/* 

Date: May 24, 2022
Title: Convert a String to a Number
Instructions: We need a function that can transform a string into a number. 

*/

const stringToNumber = function(str){
    //The parse function simply parses a string, which is a process of analyzing and transforming, into an integer.
    var num = parseInt(str);
    return num;
}